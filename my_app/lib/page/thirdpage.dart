import 'package:flutter/material.dart';

class ThirdPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
        title: Text('Ecore')),
    body: Center(
      child: Text('chưa có nội dung', style: TextStyle(fontSize: 60)
      ),
    ),
  );
}
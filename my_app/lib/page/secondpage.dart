import 'package:flutter/material.dart';

class SecondPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text('Việc quy trình')),
    body: Center(
        child: Text('chưa có nội dung', style: TextStyle(fontSize: 60)
    ),
  ),
  );
}